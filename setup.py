import streamlit as st

def setup():
    with st.expander("Setting things up"):
        st.text("to install streamlit use")
        st.code("pip install streamlit")
        st.text("to import the same into you code file use")
        st.code("import streamlit as st")
        st.text("to launch from cmd use")